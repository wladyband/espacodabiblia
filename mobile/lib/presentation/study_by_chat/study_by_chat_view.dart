import 'package:flutter/material.dart';

class StudyByChatView extends StatelessWidget {
  const StudyByChatView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
